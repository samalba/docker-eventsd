# Example file

cluster:
    foo: tcp://192.168.59.104:2375
    bar: tcp://192.168.59.105:2375

events:
    - 
        type: kill, tcp_disconnect
        from_engine: bar
        from_container: blah
        command: docker -H $ENGINE_FOO exec -- redis
        log: Hello world $ENV
